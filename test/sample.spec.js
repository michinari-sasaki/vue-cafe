describe('', () => {
  beforeAll(() => {
    // 一回だけ呼ばれる事前処理
  })

  afterAll(() => {
    // 一回だけ呼ばれる事後処理
  })

  beforeEach(() => {
    // itごとに毎回呼ばれる事前処理
  })

  afterEach(() => {
    // itごとに毎回呼ばれる事後処理
  })

  it('', () => {
    // テストの内容
  })

  it('', () => {
    // テストの内容
  })
})
